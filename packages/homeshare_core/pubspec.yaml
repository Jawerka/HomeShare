name: homeshare_core
description: Core models, outbox, hashing, disk and crypto for HomeShare.
version: 0.1.1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  crypto: ^3.0.6
  logging: ^1.3.0
  meta: ^1.15.0
  path: ^1.9.1
  uuid: ^4.5.1

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.14
